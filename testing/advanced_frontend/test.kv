fn callback() {
    print("Yielded block executed")
}

fn test_yield(c) {
    print("Inside test_yield")
    // Note: Calling a function stored in a variable might need specialized syntax if not fully implemented
}

callback()
print("Test complete")
exit(0)
